Para executar o programa:

Baixe os arquivos ou clone o git
crie o banco de dados postgres receitas_db senha:123
crie as tabelas com o schema.sql
execute npm install em frontend e backend
entre na pasta do backend com cd backend
execute npm run dev
entre na pasta do frontend com cd frontend
execute npm run dev